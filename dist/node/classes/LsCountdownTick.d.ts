export default class LsCountdownTick {
    years: string;
    days: string;
    hours: string;
    minutes: string;
    seconds: string;
    constructor({ years, days, hours, minutes, seconds }: {
        years?: string;
        days?: string;
        hours?: string;
        minutes?: string;
        seconds?: string;
    });
}
